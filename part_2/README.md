# Part 2 — MYH curated applications dataset

This folder contains the completed implementation workspace for Part 2 of the Data Pipeline Project.

## Working structure

```text
part_2/
├── instructions.txt
├── main.ipynb
├── README.md
└── data/
    ├── raw/
    │   ├── README.md
    │   ├── resultat-ansokningsomgang-2020.xlsx
    │   ├── resultat-ansokningsomgang-2021.xlsx
    │   ├── resultat-ansokningsomgang-2022.xlsx
    │   ├── resultat-ansokningsomgang-2023.xlsx
    │   ├── resultat-ansokningsomgang-2024.xlsx
    │   ├── resultat-ansokningsomgang-2025.xlsx
    │   └── .gitkeep
    └── processed/
        ├── README.md
        ├── myh_curated_applications_2020_2025.csv
        ├── myh_curated_applications_2020_2025.parquet
        └── .gitkeep
```

## Folder roles

- `main.ipynb`  
  The single assessor-friendly Part 2 notebook. It now runs from raw Excel discovery through curated-table validation, paired CSV/Parquet export, final reflection, and the SQL handoff note.

- `data/raw/`  
  Original MYH Excel workbooks for application rounds 2020–2025. These are the immutable inputs.

- `data/processed/`  
  Notebook-generated curated exports and their local export-contract documentation. These are the finished Part 2 outputs intended for the later SQL-loading phase.

## Notebook blueprint

The notebook is organized as one staged data journey:

1. Project purpose, scope, and rerun principles
2. Imports and runtime setup
3. Project paths and raw-data inventory
4. Source-file understanding
5. Why `Tabell 3` is used, and why `Tabell 4` is not merged
6. Target schema and harmonization specification
7. Reusable ingestion and standardization
8. Cleaning, normalization, and enrichment
9. Validation and quality checks
10. Export of the curated dataset
11. SQL handoff note, final reflection, and Part 2 definition-of-done check

## Part 2 completion status after Sub-project 2.7

Sub-project **2.7 — Final notebook polish and SQL handoff** closes Part 2.

The final notebook now:
- reads the six raw MYH Excel files from `data/raw/` from the start,
- preserves the chosen main-table grain: **one row = one application in one MYH application round**,
- explains why `Tabell 3` is the main table source and why `Tabell 4` is not casually merged,
- locks and implements the **32-field** curated schema,
- performs cleaning, normalization, and thoughtful enrichment without rewriting source meaning unnecessarily,
- validates the curated dataset before writing processed files,
- exports paired final deliverables:
  - `data/processed/myh_curated_applications_2020_2025.csv`,
  - `data/processed/myh_curated_applications_2020_2025.parquet`,
- re-reads both saved exports after writing to verify structural integrity and application-key safety,
- ends with a concise SQL handoff note, final reflection, and definition-of-done check.

The final full rerun/export confirmation for this handoff produced:
- **7,641 rows × 32 columns** in both CSV and Parquet,
- year coverage **2020–2025** in both exports,
- identical column order across both exports,
- **0** duplicate `(source_year, diarienummer)` keys,
- **0** missing `diarienummer` values.

The final polish matters because Part 2 is not only a collection of code cells. It is the documented evidence that the raw-source interpretation, curated schema, validation logic, and downstream handoff all fit together as one defensible pipeline.

## Raw vs processed strategy

- Raw Excel files stay unchanged in `data/raw/`.
- Processed outputs are generated by the notebook in `data/processed/`.
- The notebook remains rerunnable from the raw files without manual intervention, with PyArrow installed for the Parquet companion export.
- The canonical final Part 2 processed exports are:
  - `data/processed/myh_curated_applications_2020_2025.csv`,
  - `data/processed/myh_curated_applications_2020_2025.parquet`.
- Later SQL loading may use whichever of the paired outputs best matches the downstream loader, while the notebook keeps them equivalent at the validated table-contract level.

## Optional helper code

No separate helper module is created at this stage. The project continues to favor a clear, self-contained notebook.  
If later Part 3 database or service code grows beyond a compact next-phase implementation, that should be organized deliberately in the later phase rather than retrofitted into Part 2.

## Part 2 definition of done

Part 2 is complete because:
- `part_2/main.ipynb` reads the raw Excel files and builds the final curated dataset from scratch,
- the chosen years, chosen source table, and non-merge decision for `Tabell 4` are explained,
- source structure, target schema, harmonization rules, cleaning choices, enrichment, validation, and export rationale are documented,
- the final curated table remains schema-locked, traceable, and grain-safe,
- both final processed exports are written into `part_2/data/processed/`,
- post-write verification confirms that the exported files remain structurally correct,
- the notebook closes with a SQL handoff note and final reflection,
- the implementation is ready to continue into the later SQL database-loading phase.
